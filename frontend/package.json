{
  "name": "mediatools-frontend",
  "version": "1.0.0",
  "description": "MediaTools frontend for GitHub Pages",
  "scripts": {
    "build": "echo 'Static site - no build needed'",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
